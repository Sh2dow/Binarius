<Window x:Class="BinaryWPF.Views.CarPartsEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="Car Parts Editor" Height="600" Width="980" WindowStartupLocation="CenterScreen"
        MinWidth="820" MinHeight="520">
  <DockPanel>
    <Menu DockPanel.Dock="Top">
      <MenuItem Header="Car Parts">
        <MenuItem Header="Add Part" Command="{Binding AddPartCommand}" InputGestureText="Ctrl+A" />
        <MenuItem Header="Remove Part" Command="{Binding RemovePartCommand}" InputGestureText="Ctrl+D" />
        <MenuItem Header="Copy Part" Command="{Binding CopyPartCommand}" InputGestureText="Ctrl+C" />
        <Separator />
        <MenuItem Header="Move Up" Command="{Binding MovePartUpCommand}" InputGestureText="Ctrl+U" />
        <MenuItem Header="Move Down" Command="{Binding MovePartDownCommand}" InputGestureText="Ctrl+O" />
        <MenuItem Header="Move First" Command="{Binding MovePartFirstCommand}" />
        <MenuItem Header="Move Last" Command="{Binding MovePartLastCommand}" />
        <Separator />
        <MenuItem Header="Reverse Parts" Command="{Binding ReversePartsCommand}" />
        <MenuItem Header="Sort By Name" Command="{Binding SortPartsByNameCommand}" />
        <Separator />
        <MenuItem Header="Find and Replace" Command="{Binding FindReplaceCommand}" />
      </MenuItem>
      <MenuItem Header="Attributes">
        <MenuItem Header="Add Attribute" Command="{Binding AddAttributeCommand}" InputGestureText="Ctrl+E" />
        <MenuItem Header="Remove Attribute" Command="{Binding RemoveAttributeCommand}" InputGestureText="Ctrl+R" />
        <Separator />
        <MenuItem Header="Move Up" Command="{Binding MoveAttributeUpCommand}" />
        <MenuItem Header="Move Down" Command="{Binding MoveAttributeDownCommand}" />
        <Separator />
        <MenuItem Header="Reverse Attributes" Command="{Binding ReverseAttributesCommand}" />
        <MenuItem Header="Sort By Key" Command="{Binding SortAttributesCommand}" />
        <Separator />
        <MenuItem Header="Add Custom Attribute" Command="{Binding AddCustomAttributeCommand}" />
      </MenuItem>
      <MenuItem Header="Tools">
        <MenuItem Header="Hasher" Command="{Binding HasherCommand}" InputGestureText="Alt+H" />
        <MenuItem Header="Raider" Command="{Binding RaiderCommand}" InputGestureText="Alt+R" />
      </MenuItem>
    </Menu>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="300" />
        <ColumnDefinition Width="5" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <DockPanel Grid.Column="0">
        <TextBox DockPanel.Dock="Top" Margin="8" Text="{Binding FindText, UpdateSourceTrigger=PropertyChanged}" />
        <TreeView Margin="8,0,8,8" ItemsSource="{Binding Nodes}" SelectedItemChanged="TreeView_SelectedItemChanged">
          <TreeView.ItemTemplate>
            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
              <TextBlock Text="{Binding Name}" />
            </HierarchicalDataTemplate>
          </TreeView.ItemTemplate>
          <TreeView.Resources>
            <Style TargetType="TreeViewItem">
              <Setter Property="IsExpanded" Value="False" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                  <Setter Property="Background" Value="{DynamicResource FocusedBackBrush}" />
                  <Setter Property="Foreground" Value="{DynamicResource FocusedForeBrush}" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TreeView.Resources>
        </TreeView>
      </DockPanel>

      <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource RegBorderBrush}" />

      <wfi:WindowsFormsHost x:Name="PropertyGridHost" Grid.Column="2" Margin="8" />
    </Grid>
  </DockPanel>
</Window>
