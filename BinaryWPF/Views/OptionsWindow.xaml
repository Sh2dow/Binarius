<Window x:Class="BinaryWPF.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BinaryWPF.ViewModels.Settings"
        mc:Ignorable="d"
        Title="Settings" Height="420" Width="520" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow">
  <Window.DataContext>
    <vm:OptionsViewModel />
  </Window.DataContext>

  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Text="General" FontWeight="SemiBold" />

    <StackPanel Grid.Row="1" Margin="0,12,0,0">
      <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
        <TextBlock Text="Watermark" Width="120" VerticalAlignment="Center" />
        <TextBox Width="320" Text="{Binding Watermark, UpdateSourceTrigger=PropertyChanged}" />
      </StackPanel>

      <CheckBox Content="Enable auto backups" IsChecked="{Binding AutoBackups}" Margin="0,4,0,0" />
      <CheckBox Content="Start maximized" IsChecked="{Binding StartMaximized}" Margin="0,4,0,0" />
      <CheckBox Content="Enable experimental features" IsChecked="{Binding SoonFeature}" Margin="0,4,0,0" />
      <CheckBox Content="Disable admin warning" IsChecked="{Binding DisableAdminWarning}" Margin="0,4,0,0" />
      <CheckBox Content="Hide empty managers" IsChecked="{Binding HideEmptyManagers}" Margin="0,4,0,0" />
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="OK" Width="90" Margin="0,0,8,0" IsDefault="True" Click="OnOk" />
      <Button Content="Cancel" Width="90" IsCancel="True" />
    </StackPanel>
  </Grid>
</Window>
